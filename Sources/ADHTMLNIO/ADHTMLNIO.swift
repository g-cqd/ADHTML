// ADHTMLNIO (gated `ADHTML_NIO`) — the streaming `AsyncHTMLByteSink` over NIO `ByteBuffer` and the
// ADServe response/SSE bridge (ADR-0012). Placeholder this pass; the adapter lands after ADServe's
// view-support work (spare-parts-app ADR-0046). See `docs/adr/0012-adserve-integration.md`.
internal import ADHTMLCore

/// Namespace for the NIO byte-sink + ADServe bridge (see ADR-0012).
public enum ADHTMLNIO {}
