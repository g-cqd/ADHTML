// ADHTMLObservability (gated `ADHTML_OBS`) — optional render-path logging/metrics/tracing
// (swift-log / swift-metrics / swift-distributed-tracing). The most speculative target (ADR-0010);
// the core exposes hooks rather than forcing a backend. Placeholder this pass.
internal import ADHTMLCore

/// Namespace for optional render-path observability bindings.
public enum ADHTMLObservability {}
